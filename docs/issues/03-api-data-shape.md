# Investigation: deployment-runs API and data shape

Verify tenant and query parameters, response schema, and server-side filtering against representative run IDs. Compare direct responses for successive filter values. Check latency and errors, but keep the investigation independent from the frontend timing hypothesis.
